[project]
name = "demo"
version = "0.1.0"
description = "mlprofile - demo"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "jupyter>=1.1.1",
]
[tool.ruff.lint]
select = ["E", "F", "I"]
